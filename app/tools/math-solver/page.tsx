import MathSolver from '@/components/tools/MathSolver';

export const metadata = {
  title: 'Math & STEM Problem Solver | Academic Suite',
  description: 'Step-by-step math solver with OCR image upload.',
};

export default function MathSolverPage() {
  return <MathSolver />;
}
