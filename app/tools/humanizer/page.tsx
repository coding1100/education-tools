import Humanizer from '@/components/tools/Humanizer';

export const metadata = {
  title: 'AI Text Humanizer & Bypass | Academic Suite',
  description: 'Humanize AI drafts to produce natural, academic writing.',
};

export default function HumanizerPage() {
  return <Humanizer />;
}
